// Default output size: a 16:9 frame at Twitter/Slack's recommended link-
// preview resolution, so the exported image is share-ready with no size
// configuration required from the user.
export const OUTPUT_WIDTH = 1200;
export const OUTPUT_HEIGHT = 675;
